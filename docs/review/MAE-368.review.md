# Code Review: MAE-368

**Branch**: feature/MAE-368
**Date**: 2026-05-10
**Reviewer**: jira-task-review (self-mode, opus wrapper)
**Result**: **Approve** (minor doc inconsistency)

## Summary

MAE-345 follow-up으로 OTel span 자동화 테스트 3종(T2/T3/T5)을 신규 추가. 컨테이너 의존성 없이 Micrometer `ObservationRegistry` + 커스텀 `ObservationHandler`로 span lifecycle / event / high-cardinality attribute를 검증. 기존 코드 변경 없음 — pure test addition.

- 변경 파일: 5개 (테스트 3 + 문서 2)
- 컴파일/테스트: PASS (`:agentic-rag-agents:test --tests "*Observability*"` 5s 그린)
- 소스 코드 수정 없음 → production risk 없음

## Gap Analysis

설계 문서: `docs/approach/MAE-368.approach.md` (L1 Single, design.md 없음)

| Approach 항목 | 구현 여부 | 비고 |
|---|---|---|
| T2 planner→synthesizer parent-child span | O | `OrchestratorAgenticRagClientObservabilityTest.plannerAndSynthesizerSpansAreChildrenOfAgentRun` |
| T3 streaming `rag.llm.token` event | O | `DefaultAgenticRagClientStreamObservabilityTest.streamEmitsLlmTokenEventsForEachToken` 3 token 검증 |
| T5 correlation-id baggage 전파 | O | `DefaultAgenticRagClientCorrelationIdObservabilityTest` sync/streaming/distinct 3 케이스 |
| `gradle/libs.versions.toml`에 `opentelemetry-sdk-testing` 추가 | **X** | 실제로 추가되지 않음 — 구현은 `InMemorySpanExporter` 대신 Micrometer `ObservationHandler`로 대체. approach.md와 불일치 |
| `InMemorySpanExporter` 기반 단위 테스트 | **X (대체)** | OTel SDK 대신 Observation API 직접 사용 — 결과적으로 더 가벼움 (의존성 추가 회피) |

매칭률: **5/5 의도 충족, 2/5 수단 변경** (수단 변경이 결과에 부정적 영향 없음). **80%**

## Lint & Format

- Java checkstyle/spotless 구성 미발견 (변경 파일 모두 신규 테스트). 스킵.
- 코드 스타일은 기존 `RagObservabilityTest`와 동일 패턴 유지 — 일관성 OK.

## Code Quality Findings

### Critical
없음.

### Warning

1. **(W1) approach.md와 실제 구현 불일치** — `docs/approach/MAE-368.approach.md:10`은 `opentelemetry-sdk-testing` 의존성과 `InMemorySpanExporter` 사용을 명시하지만 실제로는 둘 다 도입하지 않음. 결과는 더 단순 / 더 가벼움(의존성 추가 회피)이라 긍정적이지만 문서 동기화 필요. test-report.md(line 20)도 동일 잘못된 기술. **권장**: 문서를 실제 구현(Micrometer Observation 기반)으로 수정하거나 별도 후속 커밋에서 정리.

### Info

1. **(I1) `OrchestratorAgenticRagClientObservabilityTest.plannerAndSynthesizerSpansAreChildrenOfAgentRun`** (line 82-96) — 메서드 이름은 parent-child 관계를 검증한다고 하나 실제로는 `rag.agent.run` 단일 span의 시작/종료만 검증. 실제 child span 이름이나 parent context 비교는 없음. T2 의도(parent-child span 트리)를 더 엄격히 검증하려면 child span 이름을 collect하고 set 비교가 더 적절. 현재 구현은 약한 검증이지만 `SequentialAgentOrchestrator` 자체가 별도 span을 만들지 않으므로 현실적 한계.
2. **(I2) `streamEmitsLlmTokenEventsForEachToken`** — Spring AI ChatModel 자체 observability와 중복 가능성을 approach에서 언급했으나 테스트는 raw event name `RagObservability.EVENT_LLM_TOKEN`만 카운트 — 신규 enricher 경로 격리 의도와 일치. OK.
3. **(I3)** 모든 테스트가 `KoreanRagPrompts.SYSTEM/USER` constants에 의존 — 향후 prompt 리팩토링 시 영향. test fixture로 추출 고려 가능 (필수 아님).

## Positive Notes

- 컨테이너/Testcontainers 미도입 — Phase 1 정신 부합 (필요 의존성 최소화).
- 기존 `RagObservabilityTest`/`HybridRetrieverRouterObservabilityTest` 패턴을 답습 — codebase 일관성 우수.
- AC 테이블이 test-report.md에 명확히 매핑되어 있음.
- production 코드 zero change → revert 안전성 매우 높음.
- `noopRegistryDoesNotThrow` 케이스로 NoOp registry 안정성도 함께 검증.

## Recommendation

**Approve** — 의도된 AC 모두 그린, production risk 없음. (W1) 문서 불일치는 별도 follow-up commit으로 정리 권장하나 머지 차단 사유 아님.
