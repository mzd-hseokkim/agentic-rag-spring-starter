# Code Review: MAE-382

**Date**: 2026-05-14
**Branch**: feature/MAE-382
**Reviewer**: jira-task-review (self-mode, model: opus)
**Result**: **Approve**

## Summary

| Item | Value |
|------|-------|
| 검토 파일 수 | 2 (main 1, test 1) |
| 설계-구현 매칭률 | ~100% (approach 기준, design.md 없음) |
| Critical | 0 |
| Warning | 0 |
| Info | 1 |
| Build/Test | `:agentic-rag-core:test` BUILD SUCCESSFUL, ToolInvocationEventTest 6/6 pass |

변경 파일:
- `agentic-rag-core/src/main/java/kr/co/mz/agenticai/core/common/event/ToolInvocationEvent.java` (신규)
- `agentic-rag-core/src/test/java/kr/co/mz/agenticai/core/common/event/ToolInvocationEventTest.java` (신규)

> 변경은 untracked 상태(미커밋). merge 단계 전 커밋 필요.

## Gap Analysis

`docs/design/MAE-382.design.md` 부재 → `docs/approach/MAE-382.approach.md` 기준으로 분석.

| Approach 항목 | 구현 여부 |
|---------------|-----------|
| `ToolInvocationEvent` — `RagEvent` 하위 marker interface (`LlmEvent` 패턴 답습) | 완료 — `extends RagEvent`, 내부 `record Started/Completed` |
| 필드: `toolName`, `argsSummary`, `startedAt`, `durationMillis`(Completed only), `truncated`, `error`(nullable) | 완료 — 전 필드 존재, `timestamp`/`correlationId`은 `RagEvent` 계약 |
| 검증 (1) record 동등성/필드 채움 | 완료 — `startedRecord_fieldsAndEquality`, `completedRecord_fieldsAndEquality` |
| 검증 (2) `ApplicationEventRagEventPublisher` 라운드트립 | 완료 — `publisher_roundTrip_startAndEndEventsDelivered` |
| 검증 (3) 예외 케이스 `error` non-null | 완료 — `completedRecord_errorFieldIsNonNullWhenToolThrew` |
| 검증 (4) `truncated=true` 출력 상한 초과 시 | 완료 — `startedRecord_truncatedTrueWhenArgsSummaryExceedsMaxLength` |
| `FileSystemToolCallbackProvider` publish 호출 통합 | 범위 외 (의도) — sibling MAE-378 미머지, approach 리스크 항목에 명시적으로 후속 commit/PR로 분리 |

매칭률: in-scope 항목 100%. task 제목의 "RagEventPublisher 통합"은 approach에서 provider 통합을 명시적으로 out-of-scope 처리 — 일관성 있음.

## Lint & Format

Java 변경 파일 대상. 프로젝트 빌드에 checkstyle task가 wiring되어 있지 않음 → lint 단계 스킵. 컴파일 및 테스트는 `./gradlew :agentic-rag-core:test`로 통과 확인.

## Code Quality Findings

### Critical
없음

### Warning
없음

### Info

- `ToolInvocationEvent.java:9` — `ARGS_SUMMARY_MAX_LENGTH` 상수가 인터페이스에 정의되어 있으나 truncation을 강제하는 production 코드는 본 PR에 없음(테스트에서만 사용). truncation은 호출자(provider) 책임이며 provider 통합이 MAE-378로 분리된 상태이므로 의도된 갭. MAE-378 머지 후 provider 측에서 실제 enforce 필요 — 후속 작업에서 누락되지 않도록 추적 권장.

## Positive Notes

- `LlmEvent` 패턴을 정확히 답습 — marker interface + nested record, 일관성 우수.
- `RagEvent`가 sealed가 아니라는 Javadoc 명시 사항을 올바르게 인지하고 "sealed 확장" 대신 marker interface 패턴 채택 (approach 핵심 결정과 일치).
- record 불변성 + 공개 API Javadoc 충실 (`@param` 전 필드 문서화, `@value` 참조 활용).
- `argsSummary`/`truncated` 필드로 PII/경로 누출 방어 설계를 타입 레벨에 반영.
- 테스트가 approach 검증 기준 4종을 1:1 커버 + record 동등성/`RagEvent` 구현 확인 2종 추가.
