# Review: MAE-374 — MetricsPanel — Actuator metrics 폴링 카드

- **Task**: MAE-374
- **Branch**: feature/MAE-374
- **Reviewer**: jira-reviewer (self-mode, model: opus)
- **Result**: **Approve**
- **설계-구현 매칭률**: 100%

## Summary

`MetricsPanel.tsx` 신규 1파일 추가. approach 문서(L1) 명시된 항목을 모두 충족.
변경 파일 수: 1 (TSX 신규) + 1 (package-lock.json 무관 정리).
ESLint / TypeScript(tsc --noEmit) 모두 통과.

## Gap Analysis

approach `docs/approach/MAE-374.approach.md` 기준:

| 항목 | 구현 위치 | 상태 |
|---|---|---|
| 변경 영역: `src/panels/MetricsPanel.tsx` 신규 1파일 | MetricsPanel.tsx | ✓ |
| `getMetric` 헬퍼 재사용 (MAE-370 산출물, 수정 없음) | line 2 import | ✓ |
| `useEffect` + `setInterval(5000ms)` 폴링 | line 84-85 | ✓ |
| 언마운트 시 cleanup | line 86-89 (`active=false` + `clearInterval`) | ✓ |
| 카드 2개 고정: `agentic.rag.llm.duration` (value+count), `agentic.rag.retrieval.hits` (value) | line 14-22, 96-120 | ✓ |
| 폴링 실패 시 마지막 성공값 유지 + "stale" 배지 | line 72-78, 117 | ✓ |
| Loading → 첫 성공 전까지 표시, 이후 실패는 stale | line 75 `stale: !prev[name].loading` | ✓ |
| 상태: 로컬 `useState<Record<name, {value, stale, ...}>>` | line 42-46 | ✓ |

매칭률: 100%.

## Lint & Format

| 도구 | 대상 | 결과 |
|---|---|---|
| ESLint (`npx eslint src/panels/MetricsPanel.tsx`) | 신규 파일 | Pass (0 issues) |
| TypeScript (`tsc --noEmit`) | 프로젝트 전체 | Pass (0 errors) |

## Code Quality Findings

### Critical
없음.

### Warning
없음.

### Info

1. **package-lock.json 무관 변경** (package-lock.json, 25줄 삭제)
   `@emnapi/core`, `@emnapi/runtime` 항목 삭제. 본 태스크 의도와 무관한 dirty change.
   Karpathy "Surgical Changes" 원칙상 별도 정리 커밋 권장 또는 commit 전 `git checkout -- package-lock.json`.
   (npm install 부수 효과로 추정 — 기능 영향 없음)

2. **measurement 통계 fallback 체인** (MetricsPanel.tsx:64)
   `TOTAL_TIME ?? VALUE ?? COUNT` 순. Counter 메트릭(`retrieval.hits`)은 COUNT만 존재하므로 최종 COUNT로 떨어지는 동작이 의도된 것임을 확인. 다만 retrieval.hits 카드는 `formatHits(s.value)`로 표시되므로 의미상 일관됨.

3. **CSS 스타일 미포함** (MetricsPanel.tsx:93-122)
   `metric-card`, `metric-card--stale`, `metric-card__badge` 등 클래스는 본 PR에 스타일 정의 없음. App 통합/스타일링은 MAE-375(레이아웃 통합) 책임으로 approach에 명시되어 있어 본 태스크 범위 외.

4. **메트릭 자동 발견 미구현** (approach Open Items 명시)
   `/actuator/metrics` 루트 나열은 별도 작업. 현재 2개 고정 카드로 요구사항 검증 목표 충족.

## Positive Notes

- `Promise.allSettled`로 메트릭 단위 부분 실패 처리 — 한 메트릭 404가 다른 카드를 깨뜨리지 않음.
- `active` flag로 언마운트 후 setState race 차단 — React strict mode 안전.
- 명시적 `MetricName` 유니온 타입으로 카드 키 타입 안전성 확보.
- approach 문서의 결정(폴링 주기, stale 정책, 카드 구성)을 코드에 정확히 반영. 추가 추상화 없음(Karpathy "Simplicity First" 부합).
- 식별자가 WHAT을 설명하여 주석 없음 — CLAUDE.md 주석 원칙 준수.
