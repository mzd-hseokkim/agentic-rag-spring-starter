# MAE-346 Code Review (round 4)

- **Date**: 2026-05-09
- **Branch**: feature/MAE-346 (working tree, uncommitted)
- **Result**: Approve
- **검토 파일 수**: 14
- **Reviewer**: jira-reviewer subagent (model: opus)

## Summary
라운드 3 Critical 완전 해소 확인. `@ActiveProfiles({"agents","rolling"})` + `application-rolling.yml`(`type=ROLLING_SUMMARY`, `token-budget=100`, `recent-turns=2`) 조합으로 `RollingSummaryPolicy` bean이 정확히 주입되며 IT 2 케이스 통과 (BUILD SUCCESSFUL). AC "RollingSummaryPolicy E2E" 충족. 잔여 Warning 3건은 비차단(non-blocking) 품질 이슈로 후속 ticket 권장.

## Gap Analysis
**설계-구현 일치율**: 100% (13 / 13).

| # | 항목 | 상태 |
|---|------|------|
| 1–11 | SPI / 정책 3종 / SummaryAgent / AutoConfig / unit tests | OK |
| 12 | IT_RollingSummary | **OK (해소)** — profile rolling + ROLLING_SUMMARY 활성, 2 케이스 통과 |
| 13 | README Memory 섹션 | OK |

## Lint & Format
| 도구 | 대상 | 결과 |
|------|------|------|
| Gradle javac/test | examples/agentic-rag-demo | BUILD SUCCESSFUL |
| 정적 리뷰 | 14 java | 컴파일 차원 결함 없음 |

## Code Quality Findings

### Critical
- 없음.

### Warning
- `agentic-rag-autoconfigure/.../AgenticRagProperties.java:258` — `Memory.historyLimit`(default 10)이 어떤 코드 경로에서도 읽히지 않음. 라운드 3 동일, **미수정**. `RecentMessagesPolicy.windowSize` 기본(10)과 우연히 일치할 뿐, 사용자가 `agentic-rag.memory.history-limit=20`을 지정해도 정책에 전달되지 않음. Approach Key Decision #6 "무중단 마이그레이션" 위반. (a) `Policy.windowSize`가 default일 때 `historyLimit` fallback, 또는 (b) `@Deprecated` + README migration note.
- `agentic-rag-core/.../RollingSummaryPolicy.java:93` — `splitAt==0` 분기에서 budget 초과 history를 침묵 통과. 라운드 3 동일, **미수정**. `recentTurns ≥ size`면 LLM context overflow가 무신호. WARN 로깅 또는 `RagEventPublisher` 이벤트 권장.
- `agentic-rag-core/.../RollingSummaryPolicy.java:118` — `chatModel.call(...).getResult().getOutput().getText()` 체인 NPE 미방어. 라운드 3 동일, **미수정**. `try/catch` 후 `[summary unavailable]` fallback + log 권장.

### Info
- `examples/.../IT_RollingSummaryPolicy.java:24` — `@MockBean`(org.springframework.boot.test.mock.mockito) Boot 3.4 deprecated. `@MockitoBean` 마이그레이션 검토.
- `examples/.../IT_RollingSummaryPolicy.java:45` — `CHAR_COUNTER` 상수 미사용. 제거 가능.
- `agentic-rag-core/.../SpringAiChatMemoryAdapter.java:27` — `DEFAULT_MAX_MESSAGES=Integer.MAX_VALUE`. properties 노출 후속.
- `agentic-rag-agents/.../SummaryAgent.java:60` — historyLimit-int ctor 2개 `@Deprecated` 부착 권장 (Phase 2 정리).

## Acceptance Criteria 검증
| AC | 상태 |
|----|------|
| 정책 3종 단위 테스트 | 충족 |
| RollingSummaryPolicy E2E | **충족** (해소) |
| SummaryAgent 통합 테스트 갱신 | 충족 |
| README Memory 섹션 + JDBC 어댑터 | 충족 |
| Spring AI ChatMemory 관계 결론 | 충족 |

## Positive Notes
- 라운드 3 Critical 정확하게 root-cause 수정 — profile activation으로 `policy.type=ROLLING_SUMMARY`가 IT 컨텍스트에 주입. 테스트 yml 분리(`src/test/resources/application-rolling.yml`)로 main 데모 동작과 격리.
- `testRuntimeOnly junit-platform-launcher` 추가로 Gradle 9.x JUnit 5 실행 안정화. 빌드 인프라 변경 1줄로 부수효과 최소.
- `shortConversationPassesThroughWithoutSummary` 음성 케이스 추가 — non-trigger path 회귀 보호. ChatModel 미호출 시 SystemMessage 미주입 명시 단언.
- `longConversation` 케이스가 `verify(atLeastOnce)` + 첫 요소 SystemMessage + `result.size<20` 3중 단언으로 RollingSummary 본질 커버.

## Round-by-Round Resolution
| 라운드 | Critical | 해소 |
|---|---|---|
| 1 | RECENT_MESSAGES ↔ RECENT 라벨 불일치 | 라운드 2 해소 |
| 3 | IT_RollingSummary가 default RECENT 정책 주입 | 라운드 4 해소 (확인) |
| 4 | 없음 | — |

라운드 3 Warning 3건(historyLimit fallback / splitAt=0 신호 / NPE 미방어)은 **모두 미수정** carry-over. 비차단이나 후속 ticket 또는 동일 PR 내 마무리 권장.

---
Reviewed by jira-reviewer subagent (model: opus)
