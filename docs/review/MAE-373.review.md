# Review: MAE-373 — SettingsPanel — Actuator env 4 토글 + 활성 프로파일

- **결과**: Approve
- **Reviewer**: wrapper agent (self-mode, opus)
- **검토 파일 수**: 2 (신규 1, 수정 1)
- **설계-구현 매칭률**: 100%

## Summary

`docs/approach/MAE-373.approach.md` L1 plan을 정확히 구현. `SettingsPanel.tsx` 신규 + `App.tsx` 마운트 2줄. 기존 `src/api/actuator.ts`의 `getEnv` 헬퍼 재사용. read-only, useState/useEffect만 사용, 글로벌 스토어 없음 — 모두 approach와 일치.

## Gap Analysis

Approach 항목별 실제 구현 확인:

| Approach 항목 | 구현 | 위치 | 상태 |
|---|---|---|---|
| `panels/SettingsPanel.tsx` 신규 | O | `examples/agentic-rag-demo/src/main/frontend/src/panels/SettingsPanel.tsx` | OK |
| `App.tsx` 마운트 1줄 추가 | O | `App.tsx` L6 import + L118 element (실제 2줄) | OK |
| `getEnv` 헬퍼 재사용 | O | `SettingsPanel.tsx:2` | OK |
| 4개 토글 키 추출 | O | `SettingsPanel.tsx:5-10` (`agentic-rag.agents.enabled` / `factcheck.enabled` / `retrieval.query.hyde.enabled` / `retrieval.query.multi-query.enabled`) | OK |
| `propertySources` 평탄화 (first-wins) | O | `SettingsPanel.tsx:19-27` | OK |
| 활성 프로파일 표시 | O | `SettingsPanel.tsx:35-37` (`env.activeProfiles` 직접 사용 — Actuator 응답 형상에 맞음) | OK |
| 프로파일 미설정 시 `default` 표기 | O | `SettingsPanel.tsx:36` | OK |
| `disabled` 체크박스 read-only | O | `SettingsPanel.tsx:74-79` (`disabled` + `readOnly`) | OK |
| 로딩/에러 상태 텍스트 | O | `SettingsPanel.tsx:53-59` | OK |

매칭률: 9/9 = 100%.

## Lint & Format

| 도구 | 대상 | 결과 |
|---|---|---|
| eslint | `SettingsPanel.tsx`, `App.tsx` | Skipped — `node_modules` 미설치 환경(npx 임시 설치 시 `@eslint/js` 해석 실패). 빌드 환경에서 `npm install && npm run lint` 권장. |
| tsc | 동일 | Skipped — 동일 사유. 타입 사용은 정합적으로 보임(`ActuatorEnv`, `ToggleKey` 추론). |

## Code Quality Findings

### Critical
없음.

### Warning
없음.

### Info
- `SettingsPanel.tsx:19-27` — `flattenEnv`가 `propertySources` 전체를 순회하며 모든 키를 평탄화. 실제 사용은 4개 키만. Actuator `env` 응답이 수백 개 키를 포함할 수 있으나, 1회 호출 + 마운트 시점 한정이라 현 구현 그대로 acceptable. (Phase 1 단순성 우선.)
- `App.tsx:118` — `<SettingsPanel />`가 `<div className="ticks">`와 `<section id="spacer">` 사이에 배치됨. 기능상 문제 없으나 후속 레이아웃 작업(MAE-374~)에서 위치 재조정 가능성. 본 작업 범위 외.
- `SettingsPanel.tsx:48-50` — `catch (err: unknown)` 후 `err instanceof Error ? err.message : String(err)` 패턴은 안전. 향후 다른 panel에서 반복되면 공용 헬퍼 추출 검토 (Phase 1 범위 외).

## Positive Notes

- approach 문서의 L1 범위를 엄격히 준수. 추측성 기능(글로벌 스토어, 토글 mutate, refresh 버튼 등) 추가 없음 — Karpathy "Simplicity First" 부합.
- 기존 `api/actuator.ts`의 `getEnv` / `ActuatorEnv` 타입 재사용 — DRY.
- `as const` + 인덱스 타입(`ToggleKey`)로 4개 키 타입 안정성 확보.
- `env.activeProfiles` 직접 사용은 approach의 "`spring.profiles.active` 추출"보다 정확한 구현 (Actuator 응답 최상위 필드가 정답).
- `readOnly` + `disabled` 둘 다 명시 — React controlled checkbox 경고 회피.
- aria-label="Settings" — 접근성 고려.
