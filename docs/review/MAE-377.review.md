# Code Review: MAE-377

- **Task**: core: WorkspaceSandbox SPI/구현 + agentic-rag.tools.fs.* properties 정의
- **Branch**: feature/MAE-377
- **Reviewer**: wrapper agent (self-mode, fresh context — separated from approach/impl)
- **Result**: **Approve**

## Summary

WorkspaceSandbox SPI + DefaultWorkspaceSandbox + GitignoreMatcher + OutputLimits + AgenticRagProperties.Fs 바인딩이 approach 문서의 4개 AC와 모듈 경계(core SPI, autoconfigure 조립) 규칙을 충실히 따른다. 신규 파일 6개, 기존 파일 수정 2개로 surgical change. Critical/Warning 없음.

## Gap Analysis

| Approach 항목 | 구현 위치 | 상태 |
|--------------|----------|------|
| AC1 — `../`/절대경로/symlink 거부 | DefaultWorkspaceSandbox.resolve: NOFOLLOW_LINKS + startsWith(root) | ✓ 매칭 |
| AC2 — root 내부 정상 resolve | DefaultWorkspaceSandbox.resolve | ✓ 매칭 |
| AC3 — respectGitignore=false 시 매처 비활성 | GitignoreMatcher.NOOP 분기 | ✓ 매칭 |
| AC4 — properties 바인딩 | AgenticRagProperties.Fs + AgenticRagToolsAutoConfiguration | ✓ 매칭 |
| 모듈 경계 (core ↔ autoconfigure) | core SPI/구현, autoconfigure에서 Bean wiring | ✓ 매칭 |
| SPI override (`@ConditionalOnMissingBean`) | AgenticRagToolsAutoConfiguration#workspaceSandbox | ✓ 매칭 |

**매칭률: 6/6 = 100%**

## Lint & Format

- Java만 변경. 프로젝트 표준 lint 설정 없음 (checkstyle 미구성) → 스킵.
- 기존 스타일 일관성 육안 확인: 패키지/import 순서, 4-space indent, brace 위치 모두 일관.

## Code Quality Findings

| Severity | File:Line | Category | Message |
|----------|-----------|----------|---------|
| Info | DefaultWorkspaceSandbox.java:47 | API semantics | `toRealPath(NOFOLLOW_LINKS)`는 마지막 컴포넌트가 미존재면 `NoSuchFileException`을 던진다. 현재 사용 시나리오(read/list)는 OK이나, 향후 write 도구가 추가될 때 별도 `resolveForCreate` 또는 try/catch 처리 필요. Phase 2 영역. |
| Info | DefaultWorkspaceSandbox.java:47 | Security | `NOFOLLOW_LINKS`는 마지막 segment만 적용 — 중간 디렉토리 symlink는 해석되지만 startsWith(root) 검사가 root 외부 escape를 차단하므로 기능적으로 안전. 의도된 동작. |
| Info | GitignoreMatcher.java:77 | Coverage | 문자 클래스 `[`/`{`/`\` 패턴 silently skip — approach 문서에 명시된 제한사항. |
| Info | GitignoreMatcher.java:60 | Behaviour | `foo/` 같은 디렉토리 전용 패턴이 파일에도 매칭 가능. approach가 "최소 패턴 한정"으로 선언했고 SecurityException 거부 방향이라 false-positive 차단 위주 → acceptable. JGit 도입 시 정합화. |

Critical/Warning 없음.

## Positive Notes

- SPI(`WorkspaceSandbox`) ↔ 기본 구현(`DefaultWorkspaceSandbox`) 분리, `@ConditionalOnMissingBean`으로 사용자 override 보장 — CLAUDE.md SPI 설계 원칙 준수.
- `OutputLimits` record + 생성자 validation (양수 강제) — 안전한 기본값 + DEFAULT 상수 노출.
- `GitignoreMatcher`는 package-private + record `Rule` 내부 한정 — 외부 API 표면 최소화.
- 테스트 13개로 4개 AC 완전 커버 + edge case (blank/null) 추가.
- 변경 범위: 신규 파일 단위 + 기존 파일 2곳 surgical edit — Karpathy "Surgical Changes" 부합.
- 주석: WHY 중심(NOFOLLOW_LINKS 전략, 미지원 패턴 의도), 공개 API에 Javadoc — CLAUDE.md 주석 규칙 부합.

## Recommendation

**Approve** — merge 진행 가능. Phase 2 후속: JGit-backed GitignoreMatcher 어댑터 검토(Open Question P2).

---
Reviewed by wrapper agent (self-mode, model: opus)
