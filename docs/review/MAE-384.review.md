# MAE-384 Code Review

**결과**: Approve
**설계-구현 매칭률**: 100% (impl 범위 항목 1, 4 — 2/2)
**검토 파일 수**: 3개
**커밋 수**: 0개 (uncommitted — `git diff HEAD` 기준)
**Reviewer**: jira-reviewer subagent (model: opus)

## Summary

MAE-384는 MAE-379가 구현한 `GrepTool`(`fs_grep`)이 `FileSystemToolCallbackProvider`에 등록되지 않아 LLM에 노출되지 않던 wiring 누락을 보완하는 버그픽스. impl 단계 산출물(작업 항목 1, 4 = 프로덕션 코드 + demo README)을 리뷰. 작업 항목 2, 3(테스트 보강)은 `/jira-task test` 단계 책임이므로 미구현이 정상.

검토 파일:
- `agentic-rag-autoconfigure/.../tools/fs/FileSystemToolCallbackProvider.java`
- `agentic-rag-autoconfigure/.../AgenticRagFsToolsAutoConfiguration.java`
- `examples/agentic-rag-demo/README.md`

## Gap Analysis

설계 문서 없음 → Jira Acceptance Criteria 중 impl 범위(항목 1, 4) 기준.

| 항목 | 상태 | 비고 |
|---|---|---|
| 1. FileSystemToolCallbackProvider에 GrepTool 등록 | ✅ 구현됨 | 생성자 `(FileSystemTools, GrepTool)`로 변경, `toolObjects(tools, grepTool)` 등록. `AgenticRagFsToolsAutoConfiguration`도 `new GrepTool(legacySandbox, limits)` 주입 — Bean wiring 완결 |
| 4. demo README fs_grep 시연 시나리오 복구 | ✅ 구현됨 | fs_glob 시나리오 뒤에 동일 포맷으로 배치, 기대 결과 문구 포함 |
| 2, 3. 테스트 보강 | ⏭ 미구현 | test 단계 책임 — 정상 |

## Lint & Format

| 도구 | 대상 파일 수 | 결과 | 주요 이슈 |
|------|------------|------|----------|
| checkstyle / spotless / spotbugs | 0 | Skip | 프로젝트에 lint 플러그인 설정 없음 (`build.gradle.kts`, `libs.versions.toml` 확인) |

## Code Quality Findings

### Critical
- 없음.

### Warnings
- `agentic-rag-autoconfigure/src/test/java/.../FileSystemToolCallbackProviderTest.java:28` — 기존 테스트가 `new FileSystemToolCallbackProvider(fsTools)` (1-arg)를 호출. 생성자가 2-arg로 변경되어 **현재 컴파일 불가**. `agentic-rag-autoconfigure` 모듈의 `test` 소스셋 전체가 빌드 실패 상태. 사전 공지대로 test 단계에서 4종 검증으로 보강하며 해소될 예정이나, 그 전까지 `:agentic-rag-autoconfigure:test`가 red인 점은 명확히 인지 필요.

### Info
- `FileSystemToolCallbackProvider.java` — Javadoc을 GrepTool 포함하도록 갱신함. MAE-380 참조 문구가 남아있으나 기존 주석 유지이므로 surgical 범위 내.
- `AgenticRagFsToolsAutoConfiguration.java:60-65` — `fileSystemToolCallbackProvider` Bean의 Javadoc이 여전히 "Exposes FileSystemTools as..."로 GrepTool 언급 없음. 동작 무관하나 Provider 쪽 Javadoc과 정합성 어긋남. 다음 편집 시 정리 권장 (이번 변경 강제 아님).

## Positive Notes

- 변경이 최소 침습적(surgical) — 생성자 1줄, `toolObjects` 1줄, import 1줄, Bean 호출 1줄. 군더더기 없음.
- 생성자에 `GrepTool`을 명시 파라미터로 받음 — `FileSystemToolCallbackProvider`가 plain Java object를 유지하고 Spring 의존성을 들이지 않는다는 클래스 설계 의도를 그대로 지킴.
- autoconfigure wiring을 함께 수정 — 생성자만 바꾸고 Bean 등록을 누락하는 흔한 실수를 피함. impl 범위가 실제로 "LLM에 노출"까지 완결됨.
- README 시나리오가 기존 fs_glob 항목과 동일한 포맷(curl + 기대 결과)을 따라 일관적.

---
Reviewed by jira-reviewer subagent (model: opus)
