# MAE-346 Code Review (round 3)

- **Date**: 2026-05-09
- **Branch**: feature/MAE-346
- **Result**: Request Changes
- **검토 파일 수**: 12
- **Reviewer**: jira-reviewer subagent (model: opus)

## Summary
1차 Critical (`RECENT_MESSAGES` ↔ `RECENT`) 해소 확인. 잔여 Critical 1건: `IT_RollingSummaryPolicy`가 default `RECENT` 정책으로 주입되어 ROLLING_SUMMARY 경로 미검증.

## Gap Analysis
**설계-구현 일치율**: 100% (13 / 13 항목 파일 존재). 단, #12는 활성화 미흡.

| # | 항목 | 상태 | 비고 |
|---|------|------|------|
| 1 | MemoryPolicy.java | OK | apply 시그니처 approach 일치 |
| 2 | TokenCounter.java | OK | int count(String) |
| 3 | HeuristicTokenCounter | OK | char/4 + ceil |
| 4 | RecentMessagesPolicy | OK | unit 테스트 통과 가능 |
| 5 | TokenWindowPolicy | OK | 0.85 safety margin |
| 6 | RollingSummaryPolicy | OK | min(splitAt, compressCount) |
| 7 | SpringAiChatMemoryAdapter | OK | get/add/clear |
| 8 | SummaryAgent | OK | policy-provided SYSTEM 감지 |
| 9 | MemoryAutoConfiguration | OK | properties + 3종 빈 |
| 10 | 3종 unit test | OK | E2E 시나리오 포함 |
| 11 | SummaryAgentTest 갱신 | OK | 정책/SystemMessage 케이스 |
| 12 | IT_RollingSummary | PARTIAL | ROLLING_SUMMARY 미활성 — Critical |
| 13 | README Memory 섹션 | OK | RECENT 라벨 수정 확인 |

## Lint & Format
| 도구 | 대상 | 결과 |
|------|------|------|
| javac | core/agents/autoconfigure | gradle 차단 — 정적 리뷰만 |
| 정적 리뷰 | 12 java | 컴파일 차원 결함 없음 |

## Code Quality Findings

### Critical
- `examples/agentic-rag-demo/src/test/java/kr/co/mz/agenticai/demo/IT_RollingSummaryPolicy.java:42` — `@Autowired MemoryPolicy`가 `application-agents.yml`에서 `policy.type` 미설정으로 기본 `RECENT`(=RecentMessagesPolicy)로 주입된다. `longConversationTriggersRollingSummaryAndFitsInBudget`은 `verify(chatModel, atLeastOnce()).call(...)`에서 실패 예상 (요약 트리거 안 됨). 수정안: (a) 테스트 전용 yml/`@TestPropertySource`에 `agentic-rag.memory.policy.type=ROLLING_SUMMARY` + 낮은 `token-budget` 지정, 또는 (b) 테스트 내부에서 `RollingSummaryPolicy`를 직접 생성하여 호출. AC "RollingSummaryPolicy E2E" 미충족.

### Warning
- `agentic-rag-autoconfigure/.../AgenticRagProperties.java:258` — Legacy `Memory.historyLimit`가 유지되나 어디서도 읽히지 않음. Approach Key Decision #6 "무중단 마이그레이션"과 모순. 기존 사용자가 `agentic-rag.memory.history-limit=20`을 지정해도 무시됨. `@Deprecated` + README migration note 추가 또는 fallback(`policy.windowSize == default ? historyLimit : policy.windowSize`) 로직 도입.
- `agentic-rag-core/.../RollingSummaryPolicy.java:91` — `splitAt=0` (recentTurns ≥ size) 분기에서 budget 초과 상태로 그대로 반환. 주석은 인지하나 런타임 신호 없음. WARN 로깅 또는 README 가이드.
- `agentic-rag-core/.../RollingSummaryPolicy.java:118` — `chatModel.call(...).getResult().getOutput().getText()` null 미방어. 일부 프로바이더 오류 시 NPE 가 agent run 전체로 escape. try/catch 후 verbatim fallback 권장.

### Info
- `agentic-rag-core/.../TokenWindowPolicy.java:22` — Javadoc의 SYSTEM 트리밍 경고는 SummaryAgent 경로에서는 발생 안 함 (history에 SYSTEM 미적재).
- `agentic-rag-core/.../SpringAiChatMemoryAdapter.java:27` — `DEFAULT_MAX_MESSAGES = Integer.MAX_VALUE`. Redis 장기 세션에서 비용. 생성자 인자/properties 노출 고려.
- `agentic-rag-agents/.../SummaryAgent.java:60` — public ctor 6개. historyLimit-int ctor 중복. `@Deprecated` 권장.

## Acceptance Criteria 검증
| AC | 상태 |
|----|------|
| 정책 3종 단위 테스트 | 충족 |
| RollingSummaryPolicy E2E | **미충족** — IT가 default RECENT |
| SummaryAgent 통합 테스트 갱신 | 충족 |
| README Memory 섹션 + JDBC 어댑터 | 충족 |
| Spring AI ChatMemory 관계 결론 | 충족 (README + 어댑터 클래스) |

## Positive Notes
- `MemoryPolicy.apply(history, query, systemPrompt)` 시그니처가 RollingSummary 요구를 흡수하면서도 단순. approach Risk #4 명시.
- `Math.min(splitAt, compressCount)` `summarizeFraction` cap 정확. 경계 단위 테스트 보호.
- `SummaryAgent` line 116-124 policy-provided SystemMessage 감지 → 중복 SYSTEM 방지. 전용 테스트 존재.
- AutoConfig 순서(`after=RedisAutoConfiguration, before=AgenticRagCoreAutoConfiguration`) — MemoryStore conditional 우선순위 정확.
- 1차 Critical(`RECENT_MESSAGES` ↔ `RECENT`) 완전 해소 — README:137, 145 모두 `RECENT`.
- `TokenCounter` / `MemoryPolicy` / `ChatMemory` 모두 `@ConditionalOnMissingBean` 일관.

---
Reviewed by jira-reviewer subagent (model: opus)
