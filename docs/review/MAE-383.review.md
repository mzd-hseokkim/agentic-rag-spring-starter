# Code Review: MAE-383 - demo 앱에 fs tools 통합 — application-tools.yml + README 시연 시나리오 + IT E2E

**Date**: 2026-05-14
**Branch**: feature/MAE-383
**Reviewed by**: jira-reviewer subagent (model: opus) — 재리뷰

## Summary

**결과**: **Approve**

직전 리뷰의 Request Changes(매칭률 60%, Critical 2건) 이후 README가 수정되어 재리뷰했다. 두 Critical이 모두 정확히 해소됐다:

1. README 툴 이름 오기 → `fs_listDir` / `fs_readFile`로 정정, 실제 `@Tool(name=...)`과 일치.
2. 미노출 툴 `fs_grep` 시연 → 실제 노출 툴 `fs_glob` 시연으로 교체.

`IT_FileSystemTools` E2E 5건은 그린이고 sandbox 탈출 차단(상대 `../`, 절대 tmpdir, Windows drive-letter)이 견고하다. 검토 파일 4개, 커밋 0개(working tree uncommitted — `main..feature/MAE-383` diff 비어 있음, 정상). Critical / Warning 없음.

## Changed Files

| 파일 | 유형 | 설명 |
|------|------|------|
| `examples/agentic-rag-demo/README.md` | 수정 | "5. 파일시스템 도구 시연" 섹션 — 툴 이름 정정 + `fs_grep`→`fs_glob` 교체 |
| `examples/agentic-rag-demo/src/main/resources/application-tools.yml` | 수정 | `agentic-rag.tools.fs` 블록 추가 (enabled/root/respect-gitignore/max-*) |
| `examples/agentic-rag-demo/src/test/java/.../demo/IT_FileSystemTools.java` | 신규 | `@SpringBootTest` E2E — fs 툴 노출 + sandbox 탈출 차단 (5 tests) |
| `examples/agentic-rag-demo/src/test/resources/application-fs.yml` | 신규 | `fs` 프로파일 테스트 리소스 (`root: ${user.dir}/../../docs`) |

## Gap Analysis

설계 문서(`docs/design/`) 부재 → `docs/approach/MAE-383.approach.md` 기준으로 분석.

**설계-구현 일치율**: **100% (6/6)**

| Approach / AC item | 구현 여부 | 위치 / 비고 |
|---------------------|----------|------|
| `application-tools.yml`에 `agentic-rag.tools.fs` 블록 추가 (properties 키 정합) | O | `application-tools.yml:11-18` — 키 6종 모두 `AgenticRagProperties.Fs`와 일치 |
| `README.md` fs 시연 시나리오 | O | `README.md:211-247` — "5. 파일시스템 도구 시연" 섹션 |
| 신규 `IT_FileSystemTools` E2E — `@SpringBootTest` + 프로파일 | O | `IT_RollingSummaryPolicy` 패턴 답습 |
| `root` 상대경로, IT는 `${user.dir}` 오버라이드로 리스크 회피 | O | `application-fs.yml` — approach 식별 리스크대로 처리 |
| `CatalogToolProvider`가 `FileSystemToolCallbackProvider` 집계 검증 | O | `IT_FileSystemTools.java:52` — `fs_glob/fs_listDir/fs_readFile` 노출 확인 |
| `fs_grep` 툴콜 정상 동작 검증 | O (정정) | approach 문구가 stale. 실제 노출 툴은 `fs_glob`/`fs_listDir`/`fs_readFile` 3종이며 IT가 이를 검증 — 구현이 올바른 방향으로 정정됨 |

직전 리뷰가 지적한 `fs_grep` 항목은 approach 문서 자체의 오류였고, README·IT 모두 실제 노출 툴 집합(`FileSystemToolCallbackProvider`)에 맞춰 정정되어 일치율 100%.

## 재리뷰 Critical 해소 검증

| 직전 Critical | 상태 |
|---|---|
| README가 `fs_list_dir` / `fs_read_file` 오기 | **해소** — `README.md:219/225/227`이 실제 `@Tool` 이름 `fs_listDir`/`fs_readFile`와 일치 (`FileSystemTools.java:98/144`). 오기 잔존 없음 |
| README가 미노출 툴 `fs_grep` 시연 | **해소** — `fs_grep` 블록을 `fs_glob` 시연으로 교체 (`README.md:234`). `FileSystemToolCallbackProvider`는 `fs_glob`/`fs_listDir`/`fs_readFile` 3종만 노출하며 README가 이와 일치 |

## Lint & Format

| 도구 | 대상 파일 수 | 결과 | 주요 이슈 |
|------|------------|------|----------|
| checkstyle | 1 (`IT_FileSystemTools.java`) | Skipped — 프로젝트에 checkstyle/spotless 설정 없음 |
| yaml lint | 2 (`.yml`) | Skipped — 도구 미설정. 수동 확인: 들여쓰기/키 정상 |

## Code Quality Findings

### Critical

없음.

### Warning

없음.

### Info

- `IT_FileSystemTools.java:52-59` — IT가 툴 *노출(이름)* 과 sandbox resolve/reject만 검증하고 실제 툴 *호출* 경로(LLM→tool→응답)는 검증하지 않음. `ChatModel` mock 환경에서 실제 툴콜 E2E는 비현실적이며 노출+sandbox 검증은 합리적 범위.
- `application-fs.yml:8` vs `application-tools.yml:15` — `respect-gitignore` 값 불일치 (테스트 `false`, 운영 `true`). 테스트가 gitignore 영향을 배제하려는 의도면 합당. 주석에 사유 명시되어 수용 가능.
- `IT_FileSystemTools.java:29` — `@ActiveProfiles({"agents","fs"})` — main 데모는 `tools` 프로필. 테스트 전용 `application-fs.yml` 분리는 의도적이고 깔끔하나 프로필명이 달라 추적 시 한 단계 더 필요.

## Positive Notes

- 두 Critical 모두 정확히 해소 — README 툴 이름이 `FileSystemTools.java`의 `@Tool(name=...)` 3종과 정확히 일치, 미노출 `fs_grep`을 실제 노출 툴 `fs_glob`으로 교체.
- `IT_FileSystemTools`가 검증하는 `fs_glob`/`fs_listDir`/`fs_readFile`가 `AgenticRagFsToolsAutoConfiguration`이 등록하는 `FileSystemToolCallbackProvider` 노출 집합과 정확히 일치.
- `application-fs.yml`의 `${user.dir}/../../docs` 주석이 approach가 지적한 상대경로 리스크를 정확히 짚고 회피책을 설명.
- `application-tools.yml`의 fs 키 6종이 `AgenticRagProperties.Fs` 바인딩 대상과 1:1 일치 (오타·미존재 키 없음).
- IT가 Windows 드라이브문자 절대경로(`sandboxBlocksWindowsDriveAbsolutePath`)까지 커버 — 크로스플랫폼 sandbox 경계 검증이 견고.
- `IT_RollingSummaryPolicy` 패턴을 일관되게 답습 — CLAUDE.md "기존 스타일 따른다" 원칙 준수.

## Conclusion

Approve. 직전 리뷰의 Critical 2건이 모두 해소됐고 설계-구현 매칭률 100%, Critical/Warning 없음. Info 3건은 모두 의도된 설계 결정이거나 후속 일괄 정리 대상으로 머지를 막지 않는다.
