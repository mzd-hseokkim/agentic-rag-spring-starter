# Code Review: MAE-381 - test: sandbox 보안 단위 테스트 + 출력 truncation 테스트

**Date**: 2026-05-14
**Branch**: feature/MAE-381
**Reviewed by**: jira-task-review (self-mode, fresh context)

## Summary

**결과**: **Approve**

신규 테스트 파일 2종(`WorkspaceSandboxTest`, `OutputLimitsTest`)을 검토했다. production 코드 무변경, 테스트 전용 추가. 빌드 그린(`WorkspaceSandboxTest` 6/6, `OutputLimitsTest` 8/8, skipped 0). symlink/junction 같은 환경 의존 케이스는 `assumeTrue`로 안전하게 처리되고, sandbox 탈출 negative case는 모두 `SecurityException`을 검증한다. Jira AC 핵심 항목 대부분 커버. 미커버 항목(maxBytes byte 단위 절단, maxChars 라인 절단)은 production API 미존재에 기인하며 Info로 기록 — Approve를 막지 않는다.

## Changed Files

| 파일 | 유형 | 설명 |
|------|------|------|
| `agentic-rag-core/src/test/java/.../common/tool/WorkspaceSandboxTest.java` | 신규 | symlink/junction 탈출 + `.gitignore` 매칭 테스트 (6 tests) |
| `agentic-rag-autoconfigure/src/test/java/.../tools/fs/OutputLimitsTest.java` | 신규 | `OutputLimits` truncation + `FileSystemTools` 통합 truncation (8 tests) |
| `docs/approach/MAE-381.approach.md` | 신규 | L1 approach 문서 |

## Gap Analysis

설계 문서(`docs/design/`) 부재 → Jira 본문 AC + approach 문서 기준으로 분석.

**설계-구현 일치율**: **86% (6/7)**

| AC / Plan item | 구현 여부 | 위치 |
|---------------------------|----------|------|
| `../` 상대경로 거부 | O (기존 `DefaultWorkspaceSandboxTest`, 본 커밋 외) | `DefaultWorkspaceSandboxTest.java:25` |
| 절대경로 root 밖 거부 | O (기존) | `DefaultWorkspaceSandboxTest.java:45` |
| symlink 탈출 거부 | O | `WorkspaceSandboxTest.java:35` |
| Windows junction 거부 (`@EnabledOnOs(WINDOWS)`) | O | `WorkspaceSandboxTest.java:78` |
| `.gitignore` 매칭 (positive/negative/negation) | O | `WorkspaceSandboxTest.java:96,107,117` |
| maxResults 초과 → truncation 라인 | O | `OutputLimitsTest.java:30,71,113` |
| maxBytes 초과 readFile → 절단 | Partial — `maxLines` 절단만 검증, byte 단위 절단 미커버 | `OutputLimitsTest.java:92` |
| maxChars 초과 라인 → 라인 단위 절단 | X — production API 부재로 미커버 | — |

차이점: `OutputLimits`(autoconfigure placeholder)는 `maxResults`/`maxLines`만 보유, `maxBytes`/`maxChars` 절단 API가 없다. core SPI `OutputLimits`는 `maxBytes` 필드를 갖지만 절단 메서드는 없다. AC의 "maxBytes/maxChars 절단"은 production 코드 측 미구현 사항(MAE-377 범위)이므로 본 테스트 태스크에서 작성 불가 — 정당한 갭.

## Lint & Format

| 도구 | 대상 파일 수 | 결과 |
|------|------------|------|
| checkstyle | 2 | Skipped — 프로젝트에 checkstyle task 미설정 (`checkstyleTest` not found) |
| 컴파일 검증 | 2 | Pass — `./gradlew test` 컴파일·실행 성공 |

프로젝트가 Java lint 도구를 구성하지 않음. 대체 검증으로 Gradle 컴파일 + 테스트 실행 그린 확인.

## Code Quality Findings

### Critical

없음.

### Warning

없음.

### Info

- `OutputLimitsTest.java:81` — `kr.co.mz.agenticai.core.autoconfigure.tools.fs.dto.DirEntry`를 FQN inline 사용. import로 정리하면 일관성 향상 (다른 타입은 import 사용). 사소.
- 네이밍: `WorkspaceSandboxTest`가 실제로는 `DefaultWorkspaceSandbox`를 테스트하며 sibling `DefaultWorkspaceSandboxTest`와 대상이 동일. javadoc이 분리 사유(symlink/junction 추가 커버)를 명시해 혼동은 완화되나, `DefaultWorkspaceSandboxEscapeTest` 류 이름이 의도를 더 명확히 전달.
- AC 갭(maxBytes byte 단위 / maxChars 라인 절단)은 production API 추가 후 후속 테스트 태스크로 보강 필요. MAE-377 의존.
- `WorkspaceSandboxTest`의 junction/symlink 테스트는 본 Windows 호스트에서 실제 실행됨(skipped 0). CI runner 권한에 따라 skip 가능하며 `assumeTrue`로 안전 처리되어 있어 정상.

## Positive Notes

- 환경 의존 케이스(symlink, NTFS junction) 처리가 견고하다 — `assumeTrue`로 미지원 환경 graceful skip, `try/finally`로 outside 디렉토리/링크 확실히 정리.
- negative case마다 `assertThatThrownBy(...).isInstanceOf(SecurityException.class)` + 메시지 검증으로 거부 사유까지 확인.
- `OutputLimitsTest`가 단위(`apply`/`applyString`)와 통합(`FileSystemTools.listDir/readFile/glob`) 두 레벨을 모두 커버 — boundary case(`applyExactlyAtLimitDoesNotTruncate`)도 포함.
- production 코드 무변경, 테스트 전용 추가 — 단일 revert 안전, approach 문서의 롤백 전략과 일치.
- CLAUDE.md 규칙 준수: 외부 컨테이너 없이 JUnit5 + AssertJ + `@TempDir`, 기존 테스트(MAE-345/368) 스타일 답습.

## Verification Commands

```bash
git diff --name-only main..feature/MAE-381
./gradlew :agentic-rag-core:test --tests '*WorkspaceSandboxTest' \
          :agentic-rag-autoconfigure:test --tests '*OutputLimitsTest' --console=plain
```

## Conclusion

Approve. 테스트 품질·커버리지 양호, 빌드 그린, production 무변경으로 리스크 낮음. 미커버 AC 2건은 production API(MAE-377) 부재에 기인한 정당한 갭으로, 해당 API 안정화 후 후속 테스트 보강을 권장. merge 진행 가능.
