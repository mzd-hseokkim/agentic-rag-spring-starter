# Code Review: MAE-342 - Token usage Micrometer 메트릭

- **Date**: 2026-05-08
- **Branch**: feature/MAE-342
- **Reviewer**: jira-reviewer (self-mode, opus)
- **Result**: **Approve**

## Summary

`MetricsRagEventListener.onLlmResponded`에 `kind=prompt` / `kind=total` summary record 2건을 추가하고 기존 `kind=completion` 경로는 유지. 단위 테스트가 세 태그(totalAmount=50/120/170)를 모두 단언. Surgical change — 변경 라인 수 = 핸들러 +15 / 테스트 +10.

## Gap Analysis

| Approach 항목 | 구현 여부 | 근거 |
|---|---|---|
| `onLlmResponded`에 `kind=prompt`/`completion`/`total` 3종 record | OK | `MetricsRagEventListener.java:72-93` |
| `LlmResponded.promptTokens()` 사용 | OK | line 79, 86 |
| `total = prompt + completion` 합산 | OK | line 86 |
| 0-value guard 유지 | OK | `> 0` 체크 line 72/79/87 |
| 테스트에 prompt/total 단언 추가 | OK | `MetricsRagEventListenerTest.java:65-70` |
| `kind=streamed` counter 유지 | OK | line 98-103 (변경 없음) |

**매칭률**: 6/6 = **100%**

## Lint & Format

- Java: 프로젝트에 모듈 단위 checkstyle/spotless 설정 없음 — 스킵.
- 기존 코드 스타일(`TAG_PROVIDER`/`TAG_MODEL` 상수, 4-space indent, `nullSafe()` 헬퍼)을 추가 코드도 그대로 따름.

## Code Quality Findings

### Critical
없음.

### Warning
없음.

### Info
- **I-1** `MetricsRagEventListener.java:86` — `totalTokens`가 `long`이므로 두 필드 모두 `long`인 한 오버플로 위험은 사실상 없으나, `kind=prompt`가 0이고 `completion>0`인 케이스에서는 `total == completion`으로 같은 값을 두 번 record 하게 됨 (대시보드에서 `prompt+completion` 합 vs `total` 비교 시 의도된 동작). 의도라면 그대로 OK.
- **I-2** `.gitignore` 변경분(diff에 포함, `LF→CRLF` warning)은 본 태스크와 직접 관련 없어 보임 — 커밋 전 분리 권장.
- **I-3** 변경분이 아직 커밋되지 않음 (`git status`상 modified). merge 단계 전 커밋 필요.

## Positive Notes

- 기존 `M_LLM_TOKENS` 상수 + `kind` 태그 패턴을 그대로 답습 — 새 메트릭 namespace 도입 없이 시계열 확장. Karpathy "Surgical Changes" 원칙 부합.
- `> 0` guard 일관 적용 — 테스트에서 `promptTokens=0L`이면 record 미호출 사실을 test report Notes에 명시.
- 테스트가 `totalAmount()`로 누적값을 직접 단언 — 단순 count 단언보다 회귀 검출력 높음.
- 외부 대시보드 호환성: 기존 `kind=completion` 시계열 그대로 유지하므로 breaking change 없음.

## Conclusion

설계-구현 일치 100%, 보안/에러 핸들링/네이밍 이슈 없음. **Approve**. merge 전 권장: (a) `.gitignore` 무관 변경 분리, (b) 변경분 커밋.
