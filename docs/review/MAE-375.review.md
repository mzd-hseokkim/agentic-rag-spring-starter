# Code Review: MAE-375 — 레이아웃/스타일 통합 + README 시연 시나리오 + 프로덕션 빌드 검증

- **Reviewer**: jira-reviewer (self-mode, opus)
- **Date**: 2026-05-12
- **Branch**: feature/MAE-375
- **Result**: **Approve**

## Summary

Approach 문서(L1)에 기술된 4 패널 통합 + 헤더 헬스 폴링 + README 데모 시나리오 모두 구현되었고, 스타일링 결정(CSS Modules)도 README에 1줄로 기록됨. 변경 범위는 surgical하며 패널 컴포넌트 내부에는 손대지 않음. tsc/vite build/eslint 정적 통과.

## Gap Analysis (Approach 매칭률)

approach 문서 부재 → `docs/approach/MAE-375.approach.md` 기반 매칭.

| Approach 항목 | 구현 위치 | 상태 |
|---|---|---|
| App.tsx 4 패널 레이아웃 교체 | App.tsx L30-57 | ✓ |
| CSS Modules 채택 (Tailwind 미채택) | styles/layout.module.css | ✓ |
| CSS Grid 2 컬럼 (좌 Chat / 우 Stack) | layout.module.css L46-67 | ✓ |
| 우측 세로 스택 (탭 미도입) | App.tsx L49-53 | ✓ |
| 헤더 health 폴링 5s | App.tsx L10-28 (useHealthPoll) | ✓ |
| `/actuator/health` 재사용 | App.tsx L16 (직접 fetch) | △ approach는 `src/api/actuator.ts` 재사용을 언급했으나 직접 fetch 인라인 |
| README 데모 시나리오 섹션 추가 | README.md +50 | ✓ |
| README CSS Modules 결정 기록 | README.md "스타일링: CSS Modules (vanilla)" | ✓ |
| README clean bootRun fallback | README.md | ✓ |
| ≥1024px 데스크탑 검증, <1024 범위 외 | layout.module.css L78-88 (max-width 1023 한 줄 처리) | ✓ |
| 패널 4개 무손상 | git diff 미포함 | ✓ |

**매칭률**: 11/11 = **100%** (1건 minor deviation은 acceptable — 5초 폴링 + 단일 호출이라 actuator 클라이언트 wrapper 도입 가치 적음)

## Lint & Format

| Tool | Result | Note |
|---|---|---|
| tsc -b --noEmit | pass | test-report 기반 |
| vite build | pass | 37 modules, static/ 배포 확인 |
| eslint App.tsx | pass | 에러/경고 0 |

## Code Quality Findings

### Critical
- 없음

### Warning
- 없음

### Info

1. **App.tsx L21 `setInterval` 미러닝 이슈 없음** — cleanup에서 clear, intervalMs 변화 시 재구독. 정상.
2. **App.tsx L16 직접 fetch** — approach 문서가 `src/api/actuator.ts` 재사용을 권고했으나 직접 fetch. 호출 1회·5s 주기로 wrapper 도입 ROI 낮음. 향후 다른 곳에서 actuator 재사용 발생 시 추출 고려.
3. **App.css L1** — 보존용 공백 파일. import 체인 추적이 까다로워질 수 있으나 본 PR은 의도된 보존이라 acceptable (Surgical Changes 원칙). 후속 정리 시 import 제거 검토.
4. **레이아웃 미커밋 상태** — 현재 working tree에 modified/untracked만 있고 commit 미존재. 머지 전에 `feat(MAE-375)` 커밋 필요.

### Security
- credential/secret 노출 없음.
- 외부 입력은 `/actuator/health` GET only — injection 위험 없음.

## Positive Notes

- approach 문서의 Open Items(P2 헬스 점 5s 통일, P3 모바일 범위 외)까지 README/CSS에 정확히 반영.
- `data-status` attribute selector로 unknown 상태에 기본(회색) 적용 — 깔끔한 3 상태 처리.
- README 시나리오가 4 패널 + 멀티턴 + 토글까지 end-to-end로 안내, 시연 그대로 가능.
- index.css의 `#root` 고정폭 1126px → 100% 변경은 본 레이아웃 전환에 필수적이며 surgical.

## Recommendation

**Approve**. 머지 전 변경사항을 커밋만 하면 됨 (현재 working tree에 stash 상태).
