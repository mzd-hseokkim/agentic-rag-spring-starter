# MAE-384 Test Report

**결과**: PASS
**날짜**: 2026-05-15
**프레임워크**: JUnit 5 + AssertJ + Spring Boot Test (Gradle)

## Summary

| 유형 | 전체 | 통과 | 실패 | 스킵 |
|------|------|------|------|------|
| Unit (`FileSystemToolCallbackProviderTest`) | 1 | 1 | 0 | 0 |
| Integration (`IT_FileSystemTools`) | 5 | 5 | 0 | 0 |
| **합계** | **6** | **6** | **0** | **0** |

## Duration

- `FileSystemToolCallbackProviderTest`: 0.887s
- `IT_FileSystemTools`: 5.226s

## 작성/수정한 테스트

impl 단계에서 분리된 테스트 보강(AC 항목 2, 3):

- `agentic-rag-autoconfigure/.../tools/fs/FileSystemToolCallbackProviderTest.java`
  - 생성자 시그니처 변경 반영 (`new FileSystemToolCallbackProvider(fsTools, grepTool)`)
  - 노출 tool 검증을 3종 → 4종(`fs_glob`, `fs_grep`, `fs_listDir`, `fs_readFile`)으로 보강
- `examples/agentic-rag-demo/.../IT_FileSystemTools.java`
  - `fsToolsExposedViaCatalogToolProvider`의 집계 검증을 3종 → 4종(`fs_grep` 포함)으로 보강
  - 클래스 Javadoc에 `fs_grep` 반영

## 실행 명령

```bash
./gradlew :agentic-rag-autoconfigure:test --tests "*FileSystemToolCallbackProviderTest" \
          :examples:agentic-rag-demo:test --tests "*IT_FileSystemTools" \
          -x frontendBuild -x npmInstall
```

> `-x frontendBuild -x npmInstall`: demo 모듈의 frontend 빌드(`rolldown` native binding `MODULE_NOT_FOUND`)는 본 task와 무관한 기존 toolchain 이슈로, 백엔드 테스트와 무관하므로 제외하고 실행.

## Failed Tests

없음.

## Acceptance Criteria 검증

- ✅ `FileSystemToolCallbackProvider`가 4종 tool 노출 — `FileSystemToolCallbackProviderTest` 통과
- ✅ `IT_FileSystemTools`가 4종(`fs_grep` 포함) 집계 검증으로 통과 — `CatalogToolProvider` 집계 확인
