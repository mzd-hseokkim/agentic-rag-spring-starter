# Test Report: MAE-383

**Date**: 2026-05-14
**Branch**: feature/MAE-383
**Command**: `./gradlew :examples:agentic-rag-demo:test --tests "kr.co.mz.agenticai.demo.IT_FileSystemTools" -x frontendBuild -x npmInstall`

## Summary

| Metric | Value |
|--------|-------|
| Total Tests | 338 (전체 모듈) / 5 (IT_FileSystemTools) |
| Passed | 333 / 5 |
| Failed | 0 |
| Skipped | 5 |
| Duration | IT_FileSystemTools: 7.3s, IT_RollingSummaryPolicy: 3.1s |
| Result | **PASS** |

## Test Strategy

- Spring Boot 통합 테스트(`@SpringBootTest`) — 실제 AutoConfiguration 스택 기동
- `@ActiveProfiles("fs")` 로 `application-fs.yml` 테스트 리소스 로드
- `application-fs.yml`에서 `root: "${user.dir}/../../docs"` — `${user.dir}` 기반 절대경로로 상대경로 리스크 회피
- 외부 의존(Ollama, EmbeddingModel, IngestionPipeline) → `@MockBean` 교체
- 전체 모듈 회귀: `./gradlew test -x frontendBuild` — 338개 전 통과

## Test Suites

### IT_FileSystemTools (IT E2E)

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| 1 | `fsToolsExposedViaCatalogToolProvider` — fs_glob/fs_listDir/fs_readFile 노출 검증 | ✓ pass | CatalogToolProvider 집계 확인 |
| 2 | `sandboxResolvesValidPath` — 유효 경로 resolve 정상 | ✓ pass | docs/ 루트 실존 확인 |
| 3 | `sandboxBlocksParentTraversal` — `../../../etc/passwd` 차단 | ✓ pass | SecurityException 또는 IOException |
| 4 | `sandboxBlocksAbsoluteEscape` — tmpdir 절대경로 차단 | ✓ pass | SecurityException |
| 5 | `sandboxBlocksWindowsDriveAbsolutePath` — `C:\Windows` 차단 | ✓ pass | SecurityException |

### 전체 모듈 회귀 (Unit/Integration)

| # | 케이스 | 결과 | 비고 |
|---|------|------|------|
| 1 | 전체 338개 테스트 (64개 XML 리포트) | ✓ pass | 실패 0, 스킵 5 |

## Failed Tests Detail

없음

## Screenshots

해당 없음

## Acceptance Criteria 매핑

| AC | 검증 방법 | 결과 |
|----|----------|------|
| CatalogToolProvider가 FileSystemToolCallbackProvider 집계 | `fsToolsExposedViaCatalogToolProvider` — tools() 결과에 fs_glob/fs_listDir/fs_readFile 포함 확인 | Pass |
| fs tool 정상 동작 | `sandboxResolvesValidPath` — docs/ 루트 resolve 성공 | Pass |
| `../` sandbox 경로 탈출 차단 3건 | AC-3a/b/c 케이스 — parent traversal / 절대 escape / Windows drive 차단 | Pass |

## Conclusion

MAE-383 정의 검증 항목 3건 모두 통과. 전체 모듈 회귀(338개) 실패 없음.

## Notes

- `application-fs.yml` 테스트 리소스에서 `root: "${user.dir}/../../docs"` 방식으로 상대경로 리스크 해소.
- `DefaultWorkspaceSandbox.resolve()`는 존재하지 않는 파일에 대해 `toRealPath()` 중 `IOException`을 throw할 수 있음 — traversal 차단 검증은 `SecurityException | IOException` 양쪽 수용.
- `@MockBean` deprecated warning (Spring Boot 3.4) — 기존 `IT_RollingSummaryPolicy`와 동일 패턴으로 일관성 유지.
