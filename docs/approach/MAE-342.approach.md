# Approach: MAE-342 - Token usage Micrometer 메트릭

- **Level**: L1 (Single)
- **Source**: N/A — discover 생략 (Phase 2 큐레이션 항목, issuetype=작업)

## Approach (L1 Single)

- **변경 영역**: `agentic-rag-autoconfigure/.../observability/MetricsRagEventListener.java` 의 `onLlmResponded` 핸들러 + 동일 모듈 테스트.
- **핵심 결정**: 기존 `agentic.rag.llm.tokens` summary 그대로 재사용하되 `kind` 태그를 `prompt`/`completion`/`total` 3종으로 확장 — `LlmResponded.promptTokens()` 와 합산 total 을 추가 record (새 메트릭/패턴 도입 X, 기존 패턴 답습).
- **검증**: `MetricsRagEventListenerTest` 에 `prompt`/`total` 태그별 summary count·sum 단언 추가 → `./gradlew :agentic-rag-autoconfigure:test` 통과.
- **리스크**: 기존 `kind=completion` 시계열을 구독하던 외부 대시보드는 영향 없음 (태그 추가일 뿐). `kind=streamed` counter 는 그대로 유지.
- **롤백**: 추가된 `record(...)` 호출 2줄과 테스트 단언만 revert 하면 원복.
