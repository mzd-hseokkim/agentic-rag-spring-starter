# MAE-377 Approach

- **Task**: core: WorkspaceSandbox SPI/구현 + agentic-rag.tools.fs.* properties 정의
- **Level**: L1 (Single)
- **Parent**: MAE-376
- **Source**: N/A — discover 생략

## Approach (L1 Single)

- **변경 영역**: `agentic-rag-core/common/tool/` 에 `WorkspaceSandbox` SPI + `DefaultWorkspaceSandbox` 기본 구현 + `OutputLimits` record 신규, `agentic-rag-autoconfigure`의 `AgenticRagProperties`에 `Tools.Fs` 서브 properties 추가.
- **핵심 결정**: `resolve()`는 `Path.toRealPath(LinkOption.NOFOLLOW_LINKS)` + `startsWith(root)` 조합으로 root 외부/symlink/`..` 차단; `.gitignore` 매칭은 자체 구현(Glob 기반 단순 매처)으로 시작하고 JGit 도입 여부는 Open Question P2로 남김(외부 의존 최소화 원칙).
- **검증**: JUnit 5 단위 테스트로 (1) `../`/절대경로/symlink 거부 (2) root 내부 정상 resolve (3) `respectGitignore=false` 시 매처 비활성 (4) properties 바인딩 — 4 케이스 통과.
- **리스크**: `.gitignore` 자체 매처가 JGit 동작과 미세 차이 가능 → 매처 구현 범위를 최소 패턴(`*`, `**`, `/` prefix, `!` negation)로 한정하고 미지원 패턴은 문서화.
- **롤백**: 신규 파일 단위 추가만 — 기존 코드 수정은 `AgenticRagProperties` 1곳뿐이므로 git revert 안전.
