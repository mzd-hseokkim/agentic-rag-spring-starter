# Approach: MAE-368 - MAE-345 follow-up: OTel span 자동화 테스트 (T2/T3/T5)

- **Level**: L1 (Single)
- **Source**: N/A — discover 생략 (Jira 본문 기준)

---

## Approach (L1 Single)

- **변경 영역**: `gradle/libs.versions.toml`(opentelemetry-sdk-testing 추가) + `agentic-rag-agents`/`agentic-rag-core` 테스트 3종 신규 (T2 planner→synthesizer parent-child, T3 streaming `rag.llm.token` event, T5 correlation-id baggage 전파).
- **핵심 결정**: 기존 `RagObservabilityTest`/`HybridRetrieverRouterObservabilityTest`/`LlmFactCheckerObservabilityTest` 패턴을 그대로 답습 — `InMemorySpanExporter` 기반 단위 테스트, 컨테이너/Testcontainers 도입 없음.
- **검증**: `./gradlew :agentic-rag-agents:test :agentic-rag-core:test` PASS, T2/T3/T5 각 1개 이상 신규 테스트 그린.
- **리스크**: streaming Flux mock에서 token event 카운트가 Spring AI ChatModel observability와 중복될 수 있음 → 신규 enricher 경로 attribute만 검증하여 격리.
- **롤백**: 테스트 + libs.versions.toml entry만 추가되므로 단일 revert로 복구 가능.
