# Approach: MAE-375 — 레이아웃/스타일 통합 + README 시연 시나리오 + 프로덕션 빌드 검증

- **Task**: MAE-375
- **Level**: L1 (Single)
- **Parent**: MAE-369 (RAG Playground UI)
- **Depends on**: MAE-371 (Chat), MAE-372 (Ingest), MAE-373 (Settings), MAE-374 (Metrics)
- **Blocks**: 없음 (Wave 3 마지막 통합 작업)
- **Source**: `docs/requirements/rag-playground-ui.requirements.md` (Sub-task 6 / Wave 3)

## L1 Single (5줄)

- **변경 영역**: `examples/agentic-rag-demo/src/main/frontend/src/App.tsx`(기존 카운터 화면 → 4 패널 레이아웃 교체), `App.css`/신규 `styles/layout.module.css`(vanilla CSS Modules), `examples/agentic-rag-demo/README.md`(데모 시나리오 섹션 추가). 패널 컴포넌트(ChatPanel/IngestPanel/SettingsPanel/MetricsPanel)는 import만 — 내부 수정 금지.
- **핵심 결정**: 스타일링은 **CSS Modules**(요구사항 권장, Tailwind는 과투자) — README에 1줄 결정 기록. 레이아웃은 CSS Grid 2 컬럼 — 좌측 ChatPanel 주역(`grid-column: span 2` 우선, ≥1024px에서 좌 1/우 1로 분할), 우측은 Ingest/Settings/Metrics를 세로 스택(탭 도입 금지 — 단일 화면 4 패널이라 과투자). 헤더는 프로젝트 이름 + `/actuator/health` 폴링 점(옵션 — 5초 주기, `src/api/actuator.ts` 재사용, 실패 시 회색 점).
- **검증**: `./gradlew :examples:agentic-rag-demo:bootRun` 단일 명령 → http://localhost:8080/ 접속 시 4 패널 모두 렌더 + README "데모 시나리오"(파일 ingest → 질의 → 멀티턴 → 설정 토글) 순서대로 수동 완주. `frontendBuild` 태스크가 `processResources`에 자동 hook되어 정적 산출물이 `src/main/resources/static/`에 배포되는지 확인.
- **리스크**: (1) Vite dev(5173) vs Spring 정적 서빙(8080) — 본 태스크는 **8080 프로덕션 경로만** 검증, dev 모드는 범위 외. (2) `frontendBuild` 캐시 stale 시 변경이 반영 안 될 수 있음 → README에 `./gradlew :examples:agentic-rag-demo:clean bootRun` fallback 1줄 명시. (3) 헤더 health 점은 옵션 — 시간 부족 시 drop, 핵심 4 패널 통합이 우선.
- **롤백**: `App.tsx`/`App.css` git revert + 신규 `layout.module.css` 삭제 + README 섹션 제거. 패널 4개는 무손상.

## Open Items

- [P2] 헤더 health 점 폴링 주기 — 5s로 통일(MetricsPanel과 동일 주기). 별도 카드로 분리하지 않음.
- [P3] 모바일 반응형 — 본 태스크 범위 외. ≥1024px 데스크탑만 검증. README에 명시.
