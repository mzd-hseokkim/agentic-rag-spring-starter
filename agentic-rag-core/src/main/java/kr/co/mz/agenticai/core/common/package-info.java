/**
 * Core domain models shared across all Agentic RAG Spring Starter modules.
 *
 * <p>Sub-packages:
 * <ul>
 *   <li>{@code event} — pipeline event types and the default
 *       {@code ApplicationEventPublisher}-backed publisher</li>
 *   <li>{@code spi} — Service Provider Interfaces for user extension</li>
 *   <li>{@code exception} — exception hierarchy</li>
 * </ul>
 */
package kr.co.mz.agenticai.core.common;
